import react from "react";
import { View } from "react-native";

const ItemSeparator = () => {
  return (
    <View
      style={{
        width: "100%",
        borderWidth: 0.6,
        borderColor: "#A9A9A9",
        marginTop: 15,
      }}
    />
  );
};

export default ItemSeparator;
